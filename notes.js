// 全局变量
let noteIdCounter = 3;
const trashBin = document.getElementById('trashBin');
const docBin = document.getElementById('docBin');
    let trashCount = 0;
let docCount = 0;

// 便签创建、渲染
function createNote(...) { ... }
function createResultNote(...) { ... }
function createContentNode(...) { ... }

// 拖拽功能
function enableDrag(note) { ... }
function onDragStart(e) { ... }
function onDrop(e) { ... }

// 删除、恢复、保存
function deleteNote(...) { ... }
function restoreNote(...) { ... }
function saveNoteToDoc(...) { ... }

// AI hint
function fetchNoteHint(...) { ... }

// 初始化示例便签
createNote(...);
